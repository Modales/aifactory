{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "OPENROUTER_API_KEY",
      "description": "OpenRouter API key (https://openrouter.ai/settings/keys) used by the post-workout LLM coach. Optional — the app boots without it; only the 'generate coach summary' feature needs it.",
      "requiredAtBoot": false
    }
  ]
}
